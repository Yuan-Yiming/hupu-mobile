{
  "name": "hupu-mobile",
  "version": "1.0.0",
  "description": "window.addEventListener('resize', function () {\r \tvar htmlWidth = document.documentElement.clientWidth || document.body.clientWidth;\r });",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --config webpack.config.js"
  },
  "author": "yuna-yiming",
  "license": "ISC",
  "dependencies": {
    "css-loader": "^2.1.1",
    "sass-loader": "^7.1.0",
    "style-loader": "^0.23.1",
    "vue": "^2.6.10",
    "webpack": "^4.30.0"
  }
}
