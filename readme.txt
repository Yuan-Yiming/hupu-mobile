# 1rem = html根标签的font-size大小；
# 根据屏幕尺寸大小，来自动更改页面元素的尺寸，rem的基准值为屏幕宽度/10；
# 动态获取屏幕宽度viewport的方法
window.addEventListener('resize', function () {
	var htmlWidth = document.documentElement.clientWidth || document.body.clientWidth;
});

# webpack构建工具
# scss编译
# aaaqqq